services:
  - type: web
    name: week10-guestbook-api
    runtime: python
    plan: free
    rootDir: spec-hs-vibe-web-class/starters/reference/week10-complete/backend
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_SERVICE_KEY
        sync: false
