# AI에게 준 프롬프트

# AI가 설명한 구조 요약

- `SocketIO(app)`을 만든 위치:
- 서버가 메시지를 받는 이벤트 이름:
- 서버가 모든 접속자에게 방송하는 코드 줄:
- 브라우저 쪽에서 소켓 연결을 만드는 줄:
- `app.run(...)` → `socketio.run(app, ...)`로 바뀌었는가:

# 내가 직접 확인한 것 (두 브라우저로 테스트)

# 막혔던 점

# 다음에 하고 싶은 것
